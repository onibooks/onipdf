import type { GlobalContext } from '../provider';
type OniPDFProps = {
    context: GlobalContext;
};
declare const OniPDF: ({ context }: OniPDFProps) => import("preact").JSX.Element;
export default OniPDF;
